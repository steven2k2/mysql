<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Meter Reader Details</title>

    <!-- Google Fonts: Roboto -->
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <!-- Google Material Icons (Outlined Version) -->
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons+Outlined" rel="stylesheet">

    <style>
        body {
            font-family: 'Roboto', sans-serif;
        }

        .navbar {
            padding: 0.5rem 1rem;
            background-color: #007bff;
        }

        .navbar .back-btn {
            color: #fff;
            font-size: 24px;
            text-decoration: none;
        }

        .info-label {
            font-weight: 500;
        }
    </style>
</head>
<body class="bg-light">

<!-- Simple Navbar with Back Arrow -->
<nav class="navbar" role="navigation">
    <a href="javascript:history.back()" class="back-btn" aria-label="Go back to previous page">
        <span class="material-icons-outlined">arrow_back</span>
    </a>
    <span class="navbar-brand text-white ms-2">Profile</span>
</nav>

<div class="container mt-5">
    <div class="row">
        <!-- Profile Section -->
        <div class="col-md-3 border-end">
            <div class="d-flex flex-column align-items-center text-center p-3 py-5">
                 <img class="rounded-circle mt-5" width="150" src="/images/<%= data.profile_picture %>" alt="Profile picture of <%= data.name %>">

                <span class="fw-bold mt-3"><%= data.name %></span>
                <span class="text-black-50"><%= data.email %></span>
                <% if (data.nickname) { %>
                    <span class="text-muted mt-2"><%= data.nickname %></span>
                <% } %>
            </div>
        </div>

        <!-- Details Section -->
        <div class="col-md-9 p-4">
            <h3>Profile Details</h3>

            <!-- Personal Information -->
            <div class="row mb-3">
                <div class="col-md-4 info-label">Phone</div>
                <div class="col-md-8"><%= data.contact_number %></div>
            </div>
            <div class="row mb-3">
                <div class="col-md-4 info-label">Email</div>
                <div class="col-md-8">
                    <a href="mailto:<%= data.email %>"><%= data.email %></a>
                </div>
            </div>
            <div class="row mb-3">
                <div class="col-md-4 info-label">Birthday</div>
                <div class="col-md-8"><%= formatDate ? formatDate(data.birthday, 'long') : data.birthday %></div>
            </div>

            <!-- Job Information -->
            <h4 class="mt-4">Job Information</h4>
            <div class="row mb-3">
                <div class="col-md-4 info-label">Job Title</div>
                <div class="col-md-8"><%= data.job_title %></div>
            </div>
            <div class="row mb-3">
                <div class="col-md-4 info-label">Phone</div>
                <div class="col-md-8"><%= data.contact_number || 'Not provided' %></div>
            </div>
            <div class="row mb-3">
                <div class="col-md-4 info-label">Department</div>
                <div class="col-md-8"><%= data.department || 'Not provided' %></div>
            </div>
            <div class="row mb-3">
                <div class="col-md-4 info-label">Hired</div>
                <div class="col-md-8"><%= formatDate ? formatDate(data.hire_date, 'long') : data.hire_date %></div>
            </div>
            <div class="row mb-3">
                <div class="col-md-4 info-label">Active Status</div>
                <div class="col-md-8"><%= data.active ? 'Active' : 'Inactive' %></div>
            </div>

            <!-- Address Information -->
            <h4 class="mt-4">Address</h4>
            <div class="row mb-3">
                <div class="col-md-12"><%= data.full_address %></div>
            </div>

            <!-- Additional Notes -->
            <h4 class="mt-4">Notes</h4>
            <div class="row mb-3">
                <div class="col-md-12"><%= data.notes %></div>
            </div>
        </div>
    </div>
</div>

<!-- jQuery and Bootstrap JS -->
<script src="https://code.jquery.com/jquery-3.6.0.min.js" crossorigin="anonymous"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>